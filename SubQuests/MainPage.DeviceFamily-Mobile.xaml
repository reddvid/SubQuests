<contract5Present:Page
    x:Class="SubQuests.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:UI="using:Microsoft.Advertising.WinRT.UI"
    xmlns:contract5NotPresent="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:contract5Present="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SubQuests.UWP"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Enabled"
    mc:Ignorable="d">

    <contract5Present:Page.Transitions>
        <contract5Present:TransitionCollection>
            <contract5Present:NavigationThemeTransition>
                <contract5Present:NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <contract5Present:CommonNavigationTransitionInfo />
                </contract5Present:NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </contract5Present:NavigationThemeTransition>
        </contract5Present:TransitionCollection>

    </contract5Present:Page.Transitions>

    <contract5Present:Page.Resources>
        <local:VisibilityControl x:Key="viscontrol" />
    </contract5Present:Page.Resources>

    <contract5Present:Grid x:Name="g_mainlayout" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <contract5Present:Grid.RowDefinitions>
            <contract5Present:RowDefinition Height="*" />
            <contract5Present:RowDefinition Height="Auto" />
            <contract5Present:RowDefinition Height="Auto" />
            <contract5Present:RowDefinition Height="Auto" />
        </contract5Present:Grid.RowDefinitions>
        <contract5Present:Grid x:Name="g_main" Margin="12">
            <contract5Present:Grid.ChildrenTransitions>
                <contract5Present:TransitionCollection>
                    <contract5Present:RepositionThemeTransition />
                </contract5Present:TransitionCollection>
            </contract5Present:Grid.ChildrenTransitions>
            <!--  HEADER  -->
            <contract5Present:TextBlock
                x:Name="tb_head"
                Grid.Row="3"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                FontWeight="Black"
                Text="Subnetting Questions" />
            <!--  QUESTION PANEL  -->
            <contract5Present:StackPanel
                x:Name="g_question"
                Margin="12,24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <contract5Present:TextBlock
                    FontWeight="Bold"
                    Foreground="LightGray"
                    Text="Question:" />
                <contract5Present:Border Height="12" />
                <contract5Present:TextBlock
                    x:Name="tb_q"
                    FontSize="18"
                    FontStyle="Italic"
                    Text="Sample question"
                    TextWrapping="Wrap" />
            </contract5Present:StackPanel>
            <!--  ANSWER PANEL  -->
            <contract5Present:StackPanel
                x:Name="g_answer"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom">
                <contract5Present:TextBlock
                    Grid.Column="0"
                    FontSize="12"
                    Text="Answer:" />
                <contract5Present:TextBlock
                    x:Name="tb_ans"
                    Grid.Column="1"
                    HorizontalAlignment="Left"
                    FontSize="12"
                    FontWeight="Bold"
                    Text="Sample answer"
                    TextWrapping="Wrap" />
            </contract5Present:StackPanel>
        </contract5Present:Grid>
        <contract5Present:StackPanel
            x:Name="sp_buttons"
            Grid.Row="1"
            Margin="12"
            VerticalAlignment="Bottom">
            <contract5Present:Button
                x:Name="btn_showans"
                Grid.Column="0"
                Height="36"
                HorizontalAlignment="Stretch"
                Click="btn_showans_Click"
                Content="Show answer" />
            <contract5Present:Border Height="6" />
            <contract5Present:Button
                x:Name="btn_refresh"
                Grid.Column="0"
                Height="36"
                HorizontalAlignment="Stretch"
                x:Load="False"
                Click="btn_refresh_Click"
                Content="Refresh" />
            <contract5Present:Border Height="6" />
            <contract5Present:Button
                x:Name="btn_next"
                Grid.Column="2"
                Height="36"
                HorizontalAlignment="Stretch"
                Click="btn_next_Click"
                Content="Next question" />
            <contract5Present:Border Height="6" />
            <contract5Present:Button
                x:Name="btn_history"
                Grid.Column="2"
                Height="36"
                HorizontalAlignment="Stretch"
                Click="btn_history_Click"
                Content="Questions history" />
        </contract5Present:StackPanel>
        <contract5Present:StackPanel
            x:Name="g_about"
            Grid.Row="2"
            Margin="12"
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom">
            <contract5Present:StackPanel Orientation="Horizontal">
                <contract5Present:HyperlinkButton
                    x:Name="btn_website"
                    Margin="0,0,6,0"
                    Padding="0"
                    Click="btn_website_Click"
                    Content="Subnetting Questions"
                    FontSize="12"
                    Style="{ThemeResource TextBlockButtonStyle}" />
                <contract5Present:TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Text="website by" />
                <contract5Present:HyperlinkButton
                    x:Name="btn_websiteauthor"
                    Margin="6,0"
                    Padding="0"
                    Click="btn_websiteauthor_Click"
                    Content="© Kim Nobay"
                    FontSize="12"
                    Style="{ThemeResource TextBlockButtonStyle}" />
            </contract5Present:StackPanel>
            <contract5Present:Border Height="4" />
            <contract5Present:StackPanel Orientation="Horizontal">
                <contract5Present:TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Text="App developed by" />
                <contract5Present:HyperlinkButton
                    x:Name="btn_dev"
                    Margin="6,1,6,0"
                    Padding="0"
                    Click="btn_dev_Click"
                    Content="Red David"
                    FontSize="12"
                    Style="{ThemeResource TextBlockButtonStyle}" />
                <contract5Present:HyperlinkButton
                    x:Name="btn_twitter"
                    Margin="6,0"
                    Padding="0"
                    Click="btn_twitter_Click">
                    <contract5Present:Image Width="16" Source="/twitter.png" />
                </contract5Present:HyperlinkButton>
                <contract5Present:HyperlinkButton
                    x:Name="btn_fb"
                    Padding="0"
                    Click="btn_fb_Click">
                    <contract5Present:Image Width="16" Source="/fb.png" />
                </contract5Present:HyperlinkButton>
            </contract5Present:StackPanel>
            <contract5Present:Border Height="4" />
            <contract5Present:StackPanel Orientation="Horizontal">
                <contract5Present:HyperlinkButton
                    x:Name="btn_donate"
                    Padding="0"
                    Click="btn_donate_Click"
                    Content="Donate via PayPal"
                    FontSize="12"
                    Style="{ThemeResource TextBlockButtonStyle}" />
                <contract5Present:HyperlinkButton
                    x:Name="btn_feedback"
                    Margin="18,0"
                    Padding="0"
                    Click="btn_feedback_Click"
                    Content="Send feedback"
                    FontSize="12"
                    Style="{ThemeResource TextBlockButtonStyle}" />
            </contract5Present:StackPanel>
        </contract5Present:StackPanel>
        <!--  AD GOES HERE  -->
        <UI:AdControl
            Grid.Row="3"
            Width="320"
            Height="50"
            HorizontalAlignment="Stretch"
            AdUnitId="1100017629"
            ApplicationId="9nplbp33n95t" />

    </contract5Present:Grid>

</contract5Present:Page>
